# Optional T4-only attention backend. Keep this out of the default Kaggle
# requirements because it compiles/installs Triton kernels and is not needed
# for normal H3 execution.
sageattention==1.0.6
triton>=3.1,<3.3
